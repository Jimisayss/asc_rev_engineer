# Ascension Reverse Engineering Tools

Branches:
- main – clean baseline
- jules_phase2 – Jules’ analysis work
- codex_analysis – Codex’s analysis work

## Setup
```bash
python -m venv venv
source venv/bin/activate   # Linux/macOS
venv\Scripts\activate      # Windows
pip install -r requirements.txt
